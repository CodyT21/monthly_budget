<!doctype html>
<html lang="en-US">
  <head>
    <title>Monthly Budget</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    <main>
      <%== yield %>
    </main>
  </body>
</html>